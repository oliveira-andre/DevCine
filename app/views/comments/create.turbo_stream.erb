<% if @comment.reply? %>
  <%= turbo_stream.append "replies_#{@comment.parent_id}" do %>
    <%= render "comments/comment", comment: @comment %>
  <% end %>
  <%# Replace the whole collapsible so it resets AND re-collapses (hides the form). %>
  <%= turbo_stream.replace "reply_wrap_#{@comment.parent_id}" do %>
    <%= render "comments/reply", video: @video, parent_id: @comment.parent_id %>
  <% end %>
<% else %>
  <%= turbo_stream.remove "comments_empty" %>
  <%= turbo_stream.prepend "comments_page_1" do %>
    <%= render "comments/comment", comment: @comment %>
  <% end %>
  <%= turbo_stream.replace "comment_form" do %>
    <%= render "comments/form", video: @video, comment: Comment.new %>
  <% end %>
<% end %>
